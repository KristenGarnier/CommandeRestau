{% extends "layout.html.twig" %}

{% block content %}
    <div class="container">
        {% for produit in produits %}
            <div class="jumbotron">
                <h1>{{ produit.nom }}</h1>

                <p>
                    {{ produit.prix |number_format(0, '.') }} €<br>
                    {{ produit.type }}
                </p>
                <img src="{{ produit.image }}" alt="photo de {{ produit.nom }}" width="20%">
            </div>
        {% endfor %}
    </div>

{% endblock %}